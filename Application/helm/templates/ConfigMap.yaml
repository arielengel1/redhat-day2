apiVersion: v1
kind: ConfigMap
metadata:
  name: '{{ .Chart.Name }}-html' #1
immutable: false
annotations:
  argocd.argoproj.io/sync-wave: '{{ .Values.argocd.syncwave.configmap }}'
data:
{{ (.Files.Glob "html/*").AsConfig | indent 2 }}